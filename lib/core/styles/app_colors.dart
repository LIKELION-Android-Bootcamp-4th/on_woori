import 'package:flutter/material.dart';


/// 디자인 컨벤션에 따른 컬러 파일입니다.
class AppColors {
    AppColors._();

    /// - Bottom Navigation Bar
    /// - Small Button ( profile... )
    static const Color primary = Color(0xFFFFA8A8);

    /// - Big Button ( Selection )
    static const Color primarySub = Color(0xFFFF9189);

    /// - Category Container
    static const Color categoryContainer = Color(0xFFF9F3EF);

    /// Category / Size Button
    static const Color categorySizeButton = Color(0xFFF7C242);

    /// Category / Size Button Unselected
    static const Color categorySizeButtonUnSelected = Color(0xFFFFD9D9);

    /// Option / State List
    static const Color optionStateList = Color(0xFFFFE3E3);

    /// Wish Button
    static const Color wishButton = Color(0xFFF44336);

    /// Edit/Delete Text Button
    static const Color editDeleteTextButton = Color(0xFF4A90E2);

    /// Divider/TextBox Line Divider
    /// 화면에 따라 Black 일 수 있음
    static const Color dividerTextBoxLineDivider = Color(0xFFC9C9C9);

    /// 회색
    static const Color grey = Color(0xFF7D7D7D);

}